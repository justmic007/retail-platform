# Notification Service Configuration
APP_ENV=development
LOG_FORMAT=pretty

NOTIFICATION_PORT=8083

# In production these would be real API keys
# For now, handlers just log — no real email/SMS is sent
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_FROM=noreply@retailplatform.com
